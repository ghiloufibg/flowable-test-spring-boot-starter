<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://flowabletest.com/diagnostics-gateway">

    <process id="diagnosticsGatewayProcess" name="Diagnostics Gateway Process" isExecutable="true">
        <startEvent id="startEvent" />
        <sequenceFlow id="flow1" sourceRef="startEvent" targetRef="amountGateway" />

        <exclusiveGateway id="amountGateway" name="Amount check" />
        <sequenceFlow id="flowHigh" sourceRef="amountGateway" targetRef="highAmountTask">
            <conditionExpression xsi:type="tFormalExpression">${amount >= 100}</conditionExpression>
        </sequenceFlow>
        <sequenceFlow id="flowLow" sourceRef="amountGateway" targetRef="lowAmountTask">
            <conditionExpression xsi:type="tFormalExpression">${amount &lt; 100}</conditionExpression>
        </sequenceFlow>

        <userTask id="highAmountTask" name="High amount review" flowable:candidateGroups="reviewers" />
        <userTask id="lowAmountTask" name="Low amount review" flowable:candidateGroups="reviewers" />
        <sequenceFlow id="flowHighEnd" sourceRef="highAmountTask" targetRef="endEvent" />
        <sequenceFlow id="flowLowEnd" sourceRef="lowAmountTask" targetRef="endEvent" />

        <endEvent id="endEvent" />
    </process>
</definitions>
